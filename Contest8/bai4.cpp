#include <bits/stdc++.h>
#define MAX_CHAR 26
using namespace std;

int min_string(string str, int k) {
    int l = str.length();
    if(k >= l)
    { // neu k lon hon do dai str
    return 0;
    }
    int F[MAX_CHAR] = {0};
    for(int i = 0; i < l; i++) {
        
    }
}

int main() {
    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        string s;
        cin >> s;
        
    }
}